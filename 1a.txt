    function getError(qty) {
      undefinedQty = false;
      zeroArray = true;
      if (typeof qty == "undefined") {
        undefinedQty = true;
      }else {
      for (i = 0; i < qty.length; i++) {
        if (qty[i] > 0) {
          zeroArray = false;
        }
      }
    }
    if(undefinedQty == true || zeroArray == false){
      alert("No Items In Cart")
    }
  }
    

app.get('/get_to_cart', (request, response) => {
    const referrer = request.headers.referer;
    let qtys = request.session.qty
    console.log("cart qtys= " + qtys);
    if (typeof qtys !== 'undefined') {
      response.redirect('./cart.html');
    } else {
        response.redirect(referrer);
    }
  });